import 'package:flutter/material.dart';

class ScreenNotfication extends StatelessWidget {
  const ScreenNotfication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
